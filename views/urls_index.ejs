<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
    <h1>URLs Page (main)</h1>
  </header>
  <ul>
    <% for (let key in urls) { %>
      <p>
        <li><%= key %>--> <%= urls[key] %></li>
        <a href="http://localhost:<%= PORT%>/urls/<%= key%>">Edit</a>
        <form method="POST" action="/urls/<%= key %>/delete">
          <button type="delete">Delete</button>
          <br />
        </form>
      </p>
    <% }; %>
  </ul>
</body>

</html>